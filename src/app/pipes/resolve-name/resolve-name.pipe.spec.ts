import { ResolveNamePipe } from './resolve-name.pipe';

describe('ResolveNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveNamePipe();
    expect(pipe).toBeTruthy();
  });
});
